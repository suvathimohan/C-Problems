#include<stdio.h>
int main(){
    int num=3337;
    if(num>=100 && num<=999){
        printf("Entered num is 3 digit's");
    }
    else{
        printf("Entered num is more than 3 digit's or less than 3 digit's!");
    }
}
